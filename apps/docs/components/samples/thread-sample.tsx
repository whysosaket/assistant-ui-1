"use client";
import { Thread } from "../assistant-ui/thread";
import { SampleFrame } from "./sample-frame";

export const ThreadSample = () => {
  return (
    <SampleFrame>
      <Thread />
    </SampleFrame>
  );
};
